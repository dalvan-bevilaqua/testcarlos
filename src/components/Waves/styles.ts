import styled from 'styled-components'

export const LeftSVG = styled.div`
  /* z-index: -1; */
  position: absolute;
  bottom: 0;
  left: 0;
  display: flex;
  pointer-events: none;
  svg {
    height: 100%;
    width: 40vw;
  }
`
export const RightSVG = styled.div`
  /* z-index: -1; */
  position: absolute;
  bottom: 0;
  right: 0;
  display: flex;
  pointer-events: none;
  svg {
    height: 100%;
    width: 40vw;
  }
`
